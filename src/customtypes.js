/**
 * Creacion de Objeto customizado para requerimiento
 * Usuario
 * @typedef {object} Usuario
 * @property {number} id Usuario id
 * @property {string} name Usuario nombre
 * @property {number | string} [age] Usuario edad (opcional)
 * @property {boolean} actividad Usuario estado
 */


 /**
  * @type {Usuario}
  */
const usuarios1 = {
    id :7,
    name : "daniel",
    age : 25,
    actividad : true
}

/**
 * @type {Usuario}
 */

const usuarios2 = {
    id :8,
    name : "ricardo",
    age : 25,
    actividad : true
}

/**
 * @type {Usuario}
 */

const usuarios3 = {
    id :9,
    name : "mercado",
    age : 25,
    actividad : false
}