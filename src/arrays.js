/**
 * Arreglo de edades general
 * @type {Array}
 */
const age = [21, 22, 23, 100];
/**
 * Arreglo de puntajes tipo numero
 * @type {Array<Number>}
 */

const score = [1.0, 2.0, 10.0];
/**
 * Arreglo de varios datosapi
 * @type {Array<Number|string|object>}
 */

const other =["hola", {"name": "place", "lvl": 100}, true]